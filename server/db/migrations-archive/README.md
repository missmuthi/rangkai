# Archived migration files

This directory holds historical generated SQL that is no longer part of the
canonical D1 deployment path.

- `server/db/migrations/` is the canonical deployable history.
- `server/db/bootstrap/fresh-install-overlay.sql` is the separate bootstrap
  overlay used only for fresh local verification.
